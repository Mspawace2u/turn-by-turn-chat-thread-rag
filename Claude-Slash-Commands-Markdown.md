## Q: 
I’m dropping in the link to an article on a site with a bunch of inline ads. Parse only the numbered list of / commands to use with Claude and the short explainer text included with it from this article. Return it back here in a md block so I can send it to my DevOps repo please. Here’s the link 🔗 https://cybercorsairs.com/26-claude-slash-commands-that-save-serious-time/Show moreShow less

## A: 
I’ll pull only the command list + the attached explainer blurbs, strip ad junk, and return it as repo-ready markdown. No side quests.

Pulled from the Cyber Corsairs article and cleaned into repo-ready markdown.   [Сyber Сorsairs ‍☠️](https://cybercorsairs.com/26-claude-slash-commands-that-save-serious-time/)  

```plain text
# Claude Slash Commands Cheat SheetSource: https://cybercorsairs.com/26-claude-slash-commands-that-save-serious-time/## Session and Conversation Control1. `/CLEAR`     Clears the current conversation history and resets context. Useful when switching topics and avoiding context bleed.2. `/COST`     Shows token usage for the current session. Helpful for monitoring spend without leaving the chat.3. `/COMPACT [instructions]`     Compresses the conversation while letting you specify which context Claude should preserve.4. `/RESUME [session]`     Reopens a previous conversation by session ID or name.5. `/BRANCH [name]`     Creates a new branch from the current conversation so you can explore a tangent without losing the main thread.6. `/REWIND`     Rolls the conversation and code back to an earlier point when an experiment goes sideways.7. `/RENAME [name]`     Renames the current session. If left blank, Claude can auto-generate a name.8. `/EXPORT [filename]`     Saves the conversation as plain text for sharing or archiving.## Model, Usage, and Plan Controls9. `/MODEL [model]`     Switches between Claude models such as Sonnet, Opus, or Haiku without leaving the chat.10. `/USAGE`     Shows plan limits and current rate-limit status.11. `/EXTRA-USAGE`     Turns on extra usage after standard limits are reached.## Project Setup and Memory12. `/INIT`     Sets up a project with a `CLAUDE.md` guide.13. `/MEMORY`     Edits `CLAUDE.md` memory files and controls auto-memory behavior.14. `/ADD-DIR <path>`     Adds a working directory so Claude can access more files during the session.## Code Review and Security15. `/DIFF`     Opens a diff viewer for uncommitted changes and per-turn diffs.16. `/SECURITY-REVIEW`     Scans pending changes for potential security issues.17. `/PLAN [description]`     Puts Claude into plan mode, optionally starting from a task description, before code gets written.## Permissions, Agents, and Skills18. `/PERMISSIONS`     Manages allow, ask, and deny rules for tool access.19. `/AGENTS`     Manages agent and sub-agent configurations.20. `/SKILLS`     Lists available built-in and custom skills.## Plugins and Connections21. `/PLUGIN`     Manages Claude Code plugins.22. `/RELOAD-PLUGINS`     Refreshes active plugins without restarting the session.23. `/MCP`     Manages MCP server connections and OAuth authentication.## Personalization24. `/CONFIG`     Opens settings for theme, model, output style, and preferences.25. `/THEME`     Changes the color theme, including light, dark, and accessibility-friendly options.26. `/COLOR [color]`     Sets the prompt bar color for the current session, useful when running multiple sessions side by side.
```

